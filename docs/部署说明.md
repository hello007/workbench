# Git Manager 部署说明

## 生产构建

```bash
wails build -clean
```

## 分发

- 整个 `build/bin/` 目录打包
- 用户直接运行 `git-manager.exe`
- 需要WebView2运行时（Windows 10+自带）

## 配置文件

- 首次运行自动创建 `data/` 目录
- 配置文件：`data/directories.json`
